{
  "buildCommand": "composer install && npm ci && npm run build",
  "outputDirectory": "public",
  "devCommand": "npm run dev",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 0 * * *"
    }
  ]
}
